import 'package:flutter_test/flutter_test.dart';

void main() {
  // Smoke test placeholder — sign_app uses CameraScreen which requires hardware.
  // Add integration tests when running on a real device or emulator.
  test('placeholder', () => expect(true, isTrue));
}
